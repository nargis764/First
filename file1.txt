first we have to initialize git using:
"git init"

The "git status" command displays the state of the working directory 
and  the staging area



The "git add" command adds a change 
in the working directory to the staging area.